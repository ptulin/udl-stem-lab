# Production environment variables
# Copy this to .env.production.local (not committed to git)

# For subpath deployment (e.g., /udl-stem-lab)
NEXT_PUBLIC_BASE_PATH=/udl-stem-lab

# For subdomain deployment, leave this empty or unset
# NEXT_PUBLIC_BASE_PATH=
